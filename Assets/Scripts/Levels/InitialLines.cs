using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InitialLines : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        TutorialAudioText.Instance.tutorialStageAudioText(3);
    }
}
