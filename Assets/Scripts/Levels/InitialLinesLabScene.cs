using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InitialLinesLabScene : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        LabAudioText.Instance.labAudioText(0);
    }
}
